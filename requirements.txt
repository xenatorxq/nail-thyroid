# ==============================================================================
# AI-Based Smart Nail Screening System for Thyroid Dysfunction
# requirements.txt — Python 3.12+
# ==============================================================================
# Install with:
#   pip install -r requirements.txt
# ==============================================================================

# ── Core Deep Learning ─────────────────────────────────────────────────────────
tensorflow>=2.16.0,<2.18.0          # TF + Keras 3 (includes tf.keras)
# tensorflow-metal>=1.1.0           # Uncomment on Apple Silicon (M1/M2/M3)

# ── Image Processing ───────────────────────────────────────────────────────────
opencv-python>=4.9.0.80             # OpenCV: image read, resize, colour conversion
Pillow>=10.3.0                      # PIL/Pillow: image I/O, Tkinter PhotoImage

# ── Numerical Computing ────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0.0                # Array operations (TF 2.x requires NumPy <2)

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.9.0                   # Training curves, image display

# ── Optional: Analysis & Reporting ────────────────────────────────────────────
scikit-learn>=1.5.0                 # Metrics: confusion matrix, classification report
pandas>=2.2.0                       # DataFrame utilities, log analysis

# ── Note: Tkinter ─────────────────────────────────────────────────────────────
# Tkinter is part of the Python standard library.
# On Linux it may need a separate system package:
#   sudo apt-get install python3-tk   (Debian/Ubuntu)
#   sudo dnf install python3-tkinter  (Fedora/RHEL)
# On macOS/Windows it is bundled with the official Python installer.
